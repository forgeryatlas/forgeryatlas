# --- macOS ---
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes

# --- iCloud Drive sync duplicates ---
# When the working folder syncs through iCloud, conflicting copies are
# saved as "original 2.pdf", "transcription 3.html", etc. They are NOT
# real source files and must never be committed.
* [0-9].pdf
* [0-9].docx
* [0-9].html
* [0-9].xlsx
* [0-9].json
* [0-9].js
* [0-9].py
* [0-9].png
* [0-9].jpg

# --- Windows ---
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# --- Linux ---
*~
.directory
.Trash-*

# --- Editors / IDEs ---
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*.swn
*.bak
*.orig

# --- Python (build script: scripts/build_documents.py) ---
.venv/
venv/
env/
ENV/
__pycache__/
*.py[cod]
*$py.class
*.egg
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
pip-log.txt
pip-delete-this-directory.txt

# --- Node (if ever added) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Logs & temp ---
*.log
*.tmp
*.temp
*.pid

# --- Local / private ---
.env
.env.local
.env.*.local
*.local
secrets.json

# --- Raw archival sources (inputs to scripts/build_documents.py) ---
# Pages only serves the generated copies under documents/, so these
# original folders don't need to ship with the site. They remain on
# disk locally for re-running the build script.
Diplomasi belgeleri/
Mahkeme belgeleri/

# --- Historical map sources (raw, input to scripts/process_historical_map.py) ---
# Pages only serves the optimized output at static/maps/, so the raw
# David Rumsey scans don't need to ship with the site.
11517024.jpg
11517024.txt
*.jp2

# NOTE:
# Generated outputs in documents/diplomatic/, documents/judicial/, and
# data/*_documents.{json,js} are intentionally tracked so GitHub Pages
# can serve them without a build step.
